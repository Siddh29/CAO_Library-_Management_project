# CAO_Library-_Management_project
We made a smart Library management system which finds minimum path needed to arrange books of any dataset given. by using Frontend(HTML, CSS, JS) and backend(using node.js) and mySQLDatabase. 
